import React from 'react';
import Swap from '@/features/swap';
const SwapPage = () => {
    return (React.createElement("main", null,
        React.createElement(Swap, null)));
};
export default SwapPage;
